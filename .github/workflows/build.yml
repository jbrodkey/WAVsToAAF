name: Build WAVsToAAF Windows

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    name: build-windows
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.14'

      - name: Extract version
        id: get_version
        shell: pwsh
        run: |
          $content = Get-Content _version.py -Raw
          if ($content -match '__version__\s*=\s*[''\"]([^''\"]+)[''\"]') {
            $version = $matches[1]
            "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          }
          else {
            Write-Error "Unable to parse version from _version.py"
          }

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          python -m pip install pyinstaller

      - name: Install and bundle FFmpeg
        shell: pwsh
        run: |
          $outDir = 'packaging\ffmpeg\bin'
          
          Write-Host "Installing FFmpeg via Chocolatey..."
          choco install ffmpeg -y
          
          # After choco install, ffmpeg becomes available in PATH
          $ffmpegPath = (where.exe ffmpeg 2>$null | Select-Object -First 1)
          $ffprobePath = (where.exe ffprobe 2>$null | Select-Object -First 1)
          
          if (-not $ffmpegPath -or -not $ffprobePath) {
            throw "FFmpeg binaries not found in PATH after Chocolatey install"
          }
          
          Write-Host "Found ffmpeg: $ffmpegPath"
          Write-Host "Found ffprobe: $ffprobePath"
          
          # Create output directory and copy just the executables
          New-Item -ItemType Directory -Path $outDir -Force | Out-Null
          Copy-Item $ffmpegPath $outDir -Force
          Copy-Item $ffprobePath $outDir -Force
          
          # Verify
          if (-not (Test-Path "$outDir\ffmpeg.exe") -or -not (Test-Path "$outDir\ffprobe.exe")) {
            throw "Failed to copy FFmpeg binaries"
          }
          
          $ffmpegSize = (Get-Item "$outDir\ffmpeg.exe").Length
          $ffprobeSize = (Get-Item "$outDir\ffprobe.exe").Length
          Write-Host "Bundled ffmpeg.exe: $('{0:N0}' -f $ffmpegSize) bytes"
          Write-Host "Bundled ffprobe.exe: $('{0:N0}' -f $ffprobeSize) bytes"

      - name: Clean previous builds
        shell: pwsh
        run: |
          if (Test-Path dist) { Remove-Item -Recurse -Force dist }
          if (Test-Path build) { Remove-Item -Recurse -Force build }

      - name: Syntax check
        run: |
          python -m py_compile wav_to_aaf.py

      - name: Generate Windows version info
        run: |
          python packaging/generate_version_info.py

      - name: Build exe (PyInstaller using spec file)
        run: |
          pyinstaller packaging/WAVsToAAF-Windows.spec

      - name: Validate built exe
        shell: pwsh
        run: |
          $exePath = "dist\WAVsToAAF.exe"
          if (-not (Test-Path $exePath)) {
            throw "Built executable not found: $exePath"
          }
          Write-Host "Dist contents:"
          Get-ChildItem dist -Recurse | Sort-Object Length | Format-Table Name,Length

          $psi = New-Object System.Diagnostics.ProcessStartInfo
          $psi.FileName = $exePath
          $psi.Arguments = "--version"
          $psi.RedirectStandardOutput = $true
          $psi.RedirectStandardError = $true
          $psi.UseShellExecute = $false
          $psi.CreateNoWindow = $true

          $process = [System.Diagnostics.Process]::Start($psi)
          $stdout = $process.StandardOutput.ReadToEnd()
          $stderr = $process.StandardError.ReadToEnd()
          $process.WaitForExit()

          Write-Host "WAVsToAAF --version stdout:"
          Write-Host $stdout
          Write-Host "WAVsToAAF --version stderr:"
          Write-Host $stderr
          Write-Host "WAVsToAAF --version exit code: $($process.ExitCode)"

          if ($process.ExitCode -ne 0) {
            if (Test-Path "$env:TEMP\wavtoaaf_startup.log") {
              Write-Host "Startup wrapper log found:"
              Get-Content "$env:TEMP\wavtoaaf_startup.log"
            }
            throw "WAVsToAAF.exe returned non-zero exit code"
          }
          if (Test-Path "$env:TEMP\wavtoaaf_startup.log") {
            Write-Host "Startup wrapper log found:"
            Get-Content "$env:TEMP\wavtoaaf_startup.log"
          }

      - name: Create distribution package
        shell: pwsh
        run: |
          $version = "${{ steps.get_version.outputs.version }}"
          $folderName = "WAVsToAAF"
          New-Item -ItemType Directory -Path "dist/$folderName" -Force
          Copy-Item dist/WAVsToAAF.exe "dist/$folderName/"
          Copy-Item docs/README_windows.md "dist/$folderName/"

      - name: Upload distribution files as artifact
        uses: actions/upload-artifact@v6
        with:
          name: WAVsToAAF_v${{ steps.get_version.outputs.version }}
          path: dist/WAVsToAAF/*

      - name: Validate Windows build
        shell: cmd
        run: |
          call validate_windows.bat
